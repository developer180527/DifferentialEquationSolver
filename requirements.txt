PySide6>=6.5
numpy>=1.24
scipy>=1.10
sympy>=1.12
matplotlib>=3.7
# Optional: enables the GPU-accelerated interactive 3D viewer.
# Without these, the app falls back to matplotlib 3D automatically.
pyvista>=0.43
pyvistaqt>=0.11
